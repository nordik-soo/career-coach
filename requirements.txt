fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pydantic-settings>=2.1
psycopg[binary,pool]>=3.1
anthropic>=0.40
httpx>=0.27
python-dotenv>=1.0
beautifulsoup4>=4.12
lxml>=5.1
rapidfuzz>=3.6
python-dateutil>=2.8
tenacity>=8.2
redis>=5.0
itsdangerous>=2.1
# Resume parsing (Sprint 1)
pdfplumber>=0.11
python-docx>=1.1
pytest>=8.0
pytest-asyncio>=0.23
# Matching v2 step 5 semantic re-ranker (soft dependency). Engine logs
# a warning and falls back to lexical-only when this isn't installed,
# so a stripped-down dev install can skip the ~700MB torch pull. See
# skillbridge/embed/service.py.
sentence-transformers>=3.0,<5.0
